% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nuisance.R
\name{estimate_nuisance}
\alias{estimate_nuisance}
\title{Estimate nuisance functions using SuperLearner with cross-fitting}
\usage{
estimate_nuisance(
  X,
  A,
  C,
  Y,
  V = 2,
  sl_lib_prop = "SL.glm",
  sl_lib_miss = "SL.glm",
  sl_lib_outcome = "SL.glm",
  seed = NULL
)
}
\arguments{
\item{X}{Matrix of covariates.}

\item{A}{Treatment indicator (0/1).}

\item{C}{Missingness indicator (1=missing, 0=observed).}

\item{Y}{Outcome (NA or value when C=0); only used when C=0 for outcome models.}

\item{V}{Number of cross-fitting folds (default 2; the calling function may use
\code{V=1} when a simple single-library SuperLearner such as \code{SL.glm} is used).}

\item{sl_lib_prop}{Character vector of SuperLearner library for propensity e (default "SL.glm").}

\item{sl_lib_miss}{Character vector of SuperLearner library for missingness pi_0, pi_1 (default "SL.glm").}

\item{sl_lib_outcome}{Character vector of SuperLearner library for outcome mu_0, mu_1 (default "SL.glm").}

\item{seed}{Optional seed for fold splits.}
}
\value{
List with components: e, pi0, pi1, mu0, mu1 (each length n), and fold_id (fold index per row).
}
\description{
Estimates e(X)=P(A=1|X), pi_0(X)=P(C=1|X,A=0), pi_1(X)=P(C=1|X,A=1),
mu_0(X)=E(Y|X,A=0,C=0), mu_1(X)=E(Y|X,A=1,C=0). Uses V-fold cross-fitting:
for each fold, fits nuisances on the training part and predicts on the held-out part.
}
