% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multiplier_bootstrap.R
\name{multiplier_bootstrap_grid}
\alias{multiplier_bootstrap_grid}
\title{Multiplier bootstrap for simultaneous inference over a parameter grid}
\usage{
multiplier_bootstrap_grid(
  mar_result,
  param_grid,
  bound_spec = c("lower", "upper", "point"),
  assumption = c("bounded_delta", "monotonicity_pos", "monotonicity_neg", "bounded_risk",
    "point_ate"),
  B = 1000,
  alpha = 0.05,
  multiplier = c("rademacher", "gaussian")
)
}
\arguments{
\item{mar_result}{Output from \code{mar_bounds()} containing \code{phi} and optionally \code{nuisance}.}

\item{param_grid}{Data.frame or list; each row (or combination) gives a parameter set. Columns must include those needed for the coefficient vector (e.g. delta_0u, delta_1u, tau, etc.). Alternatively, a list of vectors with names \code{delta_0u}, \code{delta_1u}, etc., which will be expanded via \code{expand.grid}.}

\item{bound_spec}{Character: "lower", "upper", or "point". For "lower"/"upper" the grid rows define the sensitivity parameters for that bound (e.g. delta_0u, delta_1u for monotonicity_pos). For "point" the grid gives (delta_0, delta_1, tau) for point_ate.}

\item{assumption}{Same as in \code{mar_bounds}: "bounded_delta", "monotonicity_pos", "monotonicity_neg", "bounded_risk", "point_ate".}

\item{B}{Number of multiplier bootstrap replications (default 1000).}

\item{alpha}{Significance level for simultaneous CIs (default 0.05).}

\item{multiplier}{"rademacher" (default) or "gaussian".}
}
\value{
List with \code{grid} (data.frame with columns from param_grid plus \code{estimate}, \code{se}, \code{ci_lower}, \code{ci_upper}, \code{simultaneous_lower}, \code{simultaneous_upper}), \code{boot_replicates} (B x nrow(grid) matrix of bootstrap T* values), and \code{critical_value} (estimated 1-alpha quantile of max over grid of |T*|).
}
\description{
For a grid of sensitivity parameters, each defining a linear functional b' theta
(e.g. a bound or point estimate), computes point estimates and then uses the
multiplier bootstrap to construct simultaneous confidence intervals or bands.
Let psi_i = b' phi_i be the influence function for the functional. The bootstrap
draws G_1,...,G_n iid (e.g. Rademacher or N(0,1)) and forms T* = sqrt(n) * mean(G_i * psi_i).
Repeating B times approximates the distribution of sqrt(n)(thetahat - theta).
}
